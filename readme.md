# Overlay with HTMX + ExpressJS v2

## What you need?
1. You need nodeJS (https://nodejs.org/en/download)
2. use this link: https://nodejs.org/dist/v24.12.0/node-v24.12.0-x64.msi

## To run the overlay
Just use `npm run dev`
To get to dashboard its: `http://localhost:5000/dashboard`
To get to overlay: `http://localhost:5000/overlays/*Game name*?tourneySlug=&streamName=`

## What game skins do I have?
- Riot´s 2xKO

## No name?
This time its based on Start.gg instead of manually write them in. Now it goes all the way to Grand Final Reset (via stream queue).

## No Flag?
- To mod flag, you need to go to the flags.json (create one) file and once the player name is in the file just add the flag based on the flags folder name.
- Default flag is called pirate